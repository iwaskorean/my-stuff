import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs';
import { theme } from 'src/theme/GlobalThemeProvide';

<Meta title='Color' />

# Color

## Main

<ColorPalette>
  <ColorItem
    title='theme.color.primary'
    subtitle='violetBlue600'
    colors={[theme.color.primary]}
  />
  <ColorItem
    title='theme.color.secondary'
    subtitle='red400'
    colors={[theme.color.secondary]}
  />
  <ColorItem
    title='theme.color.tertiary'
    subtitle='gray400'
    colors={[theme.color.tertiary]}
  />
  <ColorItem
    title='theme.color.black'
    subtitle='Black'
    colors={[theme.color.black]}
  />
  <ColorItem
    title='theme.color.white'
    subtitle='gray400'
    colors={[theme.color.white]}
  />
</ColorPalette>

## Colors

<ColorPalette>
  <ColorItem
    title='Gray'
    subtitle='gray100 to 900'
    colors={[
      theme.color.gray100,
      theme.color.gray200,
      theme.color.gray300,
      theme.color.gray400,
      theme.color.gray500,
      theme.color.gray600,
      theme.color.gray700,
      theme.color.gray800,
      theme.color.gray900,
    ]}
  />
  <ColorItem
    title='Red'
    subtitle='red100 to 900'
    colors={[
      theme.color.red100,
      theme.color.red200,
      theme.color.red300,
      theme.color.red400,
      theme.color.red500,
      theme.color.red600,
      theme.color.red700,
      theme.color.red800,
      theme.color.red900,
    ]}
  />
  <ColorItem
    title='Orange'
    subtitle='orange100 to 900'
    colors={[
      theme.color.orange100,
      theme.color.orange200,
      theme.color.orange300,
      theme.color.orange400,
      theme.color.orange500,
      theme.color.orange600,
      theme.color.orange700,
      theme.color.orange800,
      theme.color.orange900,
    ]}
  />
  <ColorItem
    title='Magenta'
    subtitle='magenta100 to 900'
    colors={[
      theme.color.magenta100,
      theme.color.magenta200,
      theme.color.magenta300,
      theme.color.magenta400,
      theme.color.magenta500,
      theme.color.magenta600,
      theme.color.magenta700,
      theme.color.magenta800,
      theme.color.magenta900,
    ]}
  />
  <ColorItem
    title='Cyan'
    subtitle='cyan100 to 900'
    colors={[
      theme.color.cyan100,
      theme.color.cyan200,
      theme.color.cyan300,
      theme.color.cyan400,
      theme.color.cyan500,
      theme.color.cyan600,
      theme.color.cyan700,
      theme.color.cyan800,
      theme.color.cyan900,
    ]}
  />
  <ColorItem
    title='Yellow'
    subtitle='yellow100 to 900'
    colors={[
      theme.color.yellow100,
      theme.color.yellow200,
      theme.color.yellow300,
      theme.color.yellow400,
      theme.color.yellow500,
      theme.color.yellow600,
      theme.color.yellow700,
      theme.color.yellow800,
      theme.color.yellow900,
    ]}
  />
  <ColorItem
    title='Lime'
    subtitle='lime100 to 900'
    colors={[
      theme.color.lime100,
      theme.color.lime200,
      theme.color.lime300,
      theme.color.lime400,
      theme.color.lime500,
      theme.color.lime600,
      theme.color.lime700,
      theme.color.lime800,
      theme.color.lime900,
    ]}
  />
  <ColorItem
    title='Gold'
    subtitle='gold100 to 900'
    colors={[
      theme.color.gold100,
      theme.color.gold200,
      theme.color.gold300,
      theme.color.gold400,
      theme.color.gold500,
      theme.color.gold600,
      theme.color.gold700,
      theme.color.gold800,
      theme.color.gold900,
    ]}
  />
  <ColorItem
    title='Violet Blue'
    subtitle='violetBlue100 to 900'
    colors={[
      theme.color.violetBlue100,
      theme.color.violetBlue200,
      theme.color.violetBlue300,
      theme.color.violetBlue400,
      theme.color.violetBlue500,
      theme.color.violetBlue600,
      theme.color.violetBlue700,
      theme.color.violetBlue800,
      theme.color.violetBlue900,
    ]}
  />
  <ColorItem
    title='Blue'
    subtitle='blue100 to 900'
    colors={[
      theme.color.blue200,
      theme.color.blue300,
      theme.color.blue400,
      theme.color.blue500,
      theme.color.blue600,
      theme.color.blue700,
      theme.color.blue800,
      theme.color.blue900,
    ]}
  />
  <ColorItem
    title='Green'
    subtitle='green100 to 900'
    colors={[
      theme.color.green100,
      theme.color.green200,
      theme.color.green300,
      theme.color.green400,
      theme.color.green500,
      theme.color.green600,
      theme.color.green700,
      theme.color.green800,
      theme.color.green900,
    ]}
  />
</ColorPalette>
